[package]
name = "demo_core"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = { version = "0.10.1", default-features = false, features = ["std_rng"] }
tiny_qsim = { path = "../.." }

[lib]
crate-type = ["cdylib"]
